# STATE

## Decisions

### AD-001 — Fronteiras do workspace

- **Status:** active
- **Date:** 2026-08-08
- **Decision:** o workspace terá sete raízes autossuficientes: `payment-contracts`, `payment-api`, `payment-sbus`, `payment-core-mock`, `feature-control`, `async-redis-service` e `sandbox`; exemplos ficam dentro de `feature-control`.
- **Rationale:** separa ownership, release e documentação sem promover demonstrações a produtos.

### AD-002 — Dependências cross-boundary

- **Status:** active
- **Date:** 2026-08-08
- **Decision:** contratos e bibliotecas serão consumidos por coordenadas Maven versionadas; composite build será somente uma conveniência local explícita e os gates de release testarão artefatos publicados.
- **Rationale:** reproduz repositórios independentes e impede acoplamento invisível ao source tree.

### AD-003 — Ownership da infraestrutura local

- **Status:** active
- **Date:** 2026-08-08
- **Decision:** somente `/sandbox` criará Kafka, Redis, PostgreSQL, Registry e observabilidade; aplicações usarão Composes próprios conectados a uma rede externa do sandbox.
- **Rationale:** evita infraestrutura duplicada e permite iniciar/escalar aplicações de forma independente.

### AD-004 — Estratégia de migração

- **Status:** active
- **Date:** 2026-08-08
- **Decision:** a migração será incremental e orientada a artefatos, com equivalência e rollback por fronteira; repositórios remotos serão criados somente em iniciativa posterior.
- **Rationale:** reduz blast radius e torna regressões atribuíveis.

### AD-005 — Critério de produção

- **Status:** active
- **Date:** 2026-08-08
- **Decision:** `payment-api`, `payment-sbus`, `payment-contracts`, `feature-control` e `async-redis-service` terão gates produtivos; `payment-core-mock`, `feature-demo` e `pilot-app` serão explicitamente `NON_PRODUCTION`. Nenhum claim de prontidão vale sem relatório datado.
- **Rationale:** separa evidência operacional de finalidade didática.

### AD-006 — Meta de capacidade e saturação

- **Status:** superseded by AD-007
- **Date:** 2026-08-08
- **Decision:** o gate alvo será 10.000 req/min por 15 minutos e spike de 20.000 req/min por 60 segundos; excesso terá `429`, `202` ou buffering limitado, nunca perda silenciosa aceita.
- **Rationale:** transforma “milhares por minuto” em capacidade mensurável e trata limites downstream como restrição real.

### AD-007 — Recalibração da meta de capacidade e saturação (supersede AD-006)

- **Status:** active
- **Date:** 2026-08-14
- **Decision:** o gate alvo passa a ser 1.000 req/min sustentado por 15 minutos e spike de 2.000 req/min por 60 segundos, com latência média ≤ 300ms e p99 ≤ 10s (novo, não existia em AD-006). Admissão recalibrada: rota 20/s (era 200/s), tenant 10/s (era 50/s); o Core (`sbus.core.limit-for-period: 50`) fica inalterado, agora com 3× de folga em vez de ser o gargalo. Excesso continua `429`, `202` ou buffering limitado — nunca perda silenciosa aceita.
- **Rationale:** decisão do usuário ("limite esperado esta alto, 10mil, pode ser 1000 por min") — o alvo de AD-006 estava alto demais para o propósito real do sistema. Junto com isso, a auditoria adversarial de 2026-08-13 invalidou o relatório CAP-02 vigente: ele media o rate limiter de um único tenant (50/s admitidos, 70% de 429), não capacidade do sistema, porque o k6 usava uma única API key. A recalibração da admissão e um gate honesto (k6 com ≥2 tenants, veredito reprova quando `429 > 1%` do steady) andam juntos — um alvo novo sem um gate honesto só trocaria o número errado por outro.

### AD-008 — Tenant como conceito de domínio de primeira classe

- **Status:** active
- **Date:** 2026-08-21
- **Decision:** o tenant efetivo de uma requisição é resolvido do binding credencial→tenants (`payment.security.tenants`, hash da API key → lista de tenants); o header declarativo `X-Tenant-Id` é sempre revalidado contra esse binding, nunca aceito como asserção de identidade por si só — comportamento idêntico com ou sem o gateway na frente (o Envoy só injeta o header a partir do claim JWT como defesa em profundidade adicional, T46/TEN-07). Idempotência, fingerprint, chaves de status e o `tenantId` do envelope de eventos são sempre compostos com o tenant efetivo; unicidade de idempotência no `payment-sbus` é por `(tenant_id, idempotency_key)`, nunca chave global.
- **Rationale:** ancorar identidade na credencial (não no header) impede que um cliente force o tenant de outro; a revisão arquitetural 2026-08 encontrou vazamento cross-tenant como achado crítico (tenant B podia reutilizar a `Idempotency-Key` de A e herdar seu `requestId`/resultado/`409`) precisamente porque a idempotência anterior não tinha escopo de tenant algum.

### AD-009 — `Idempotency-Key` obrigatória no contrato público

- **Status:** active
- **Date:** 2026-08-21
- **Decision:** toda rota HTTP de escrita (`POST /payment-simulations`, `POST /v0/payment-simulations`) exige o header `Idempotency-Key` (`[A-Za-z0-9_-]{1,128}`), respondendo `400` quando ausente ou fora do padrão. A janela de idempotência efetiva é 24h (`payment.simulation.idempotency-ttl`), acima do TTL de status em cache (15m, `payment.simulation.status-ttl`).
- **Rationale:** chave opcional tornava toda a proteção de idempotência elidível por quem simplesmente não a enviasse — o segundo achado crítico da revisão 2026-08. Tornar o header obrigatório fecha esse desvio sem introduzir uma chave gerada silenciosamente pelo servidor, que o cliente não controlaria e não poderia usar para detectar sua própria duplicata.

### AD-010 — Estratégia de versionamento de API e de eventos

- **Status:** active
- **Date:** 2026-08-21
- **Decision:** o path HTTP carrega o major somente quando diverge do major estável implícito corrente (hoje: `/payment-simulations` sem prefixo é a major estável; `/v0/payment-simulations` é uma major experimental anterior, gated por feature flag); toda rota que não for a major estável implícita emite `X-Api-Version` na resposta. No contrato de eventos, `eventVersion` é `major.minor` e cada consumer valida o major via `EnvelopeVersions.assertKnownMajor(...)`; major desconhecido é poison (DLQ direta), nunca processado às cegas. Detalhe completo e alternativas consideradas: [payment-api ADR-0002](../payment-api/docs/adr/0002-api-and-event-versioning-strategy.md).
- **Rationale:** reaproveita o padrão `/v0`+header já emitidos (menor superfície nova); path só para major evita path explosion por mudanças menores; classificar major desconhecido como poison em vez de ignorar ou crashar torna um deploy divergente entre produtor/consumidor detectável, nunca processado às cegas.

## Handoff

- **Feature**: `review-2026-08-remediation` (`.specs/features/review-2026-08-remediation/{spec,context,design,tasks}.md`) — **in progress**, continuation session. The prior branch's PR (#64) merged T1–T45 into `main`; this session works on a fresh branch off that merge, closing out Phase 9 (T46–T50) plus the mandatory Verifier.
- **Phase / Task**: T46, T47, T48 done and pushed. **T49 (Gates de workspace e reconciliação) is in progress** — structural gates all green (see below); the Docker-dependent consolidated verification pass (deferred by every prior session per the lesson below) is now actually running, since this session has working Docker for the first time. `payment-sbus`'s full `-PwithIT` suite is now clean modulo 2 documented, non-blocking known gaps (below); `payment-api`'s is running next, then `payment-core-mock`, `feature-control`, `async-redis-service`, `payment-contracts` (already clean).
- **Completed this session**: T46 (`646cb3a` + 2 infra fixes `10ea7f3`/`d75e20c` + docs-policy fix `640e279`), T47 (`24a0d57`), T48 (`375a839`), equivalence manifest reconciliation (`e0ba0ef`), and 4 real bugs found+fixed while running T49's IT suites for the first time (`9168b37`, `4f6040f`, `c0e54cf` — see below). All pushed to `claude/tlc-spec-driven-continuation-f7t6hp`.
- **Real bugs found and fixed by actually running `-PwithIT` for the first time** (T49 is the first time any task in this feature has run Docker-dependent tests — every prior task from T21 onward was gated on unit tests only, per the deferral lesson below; this is exactly the risk that lesson flagged):
  1. **`gateway` compose never actually booted before this session**: `ENVOY_UID`/`ENVOY_GID` env vars + `read_only: true` + `cap_drop: ALL` crash-looped the entrypoint (usermod/groupmod on a read-only FS); fixed with `user: 101:101` instead (`10ea7f3`). Once it booted, Envoy itself then rejected `envoy.yaml`: `rate_limits` was declared as a sibling of `route:` on 3 routes instead of nested inside it (`RouteAction.rate_limits`, not `Route.rate_limits`) — fixed, plus the parity script/fixture that mirrored the same wrong nesting (`d75e20c`). T46's live smoke (including the new forged-`X-Tenant-Id` assertion) now genuinely passes against real Docker.
  2. **`payment-core-mock`'s `contractsVersion` pin (0.1.0) was never bumped** when T3 bumped `payment-contracts` to 0.2.0 — its build (CI included) has been broken since T3 landed, undetected because no task's gate ever built that boundary (`3a27294`).
  3. **`HikariPoolHealthIndicator` (payment-sbus) had neither `@Readiness` nor `@Liveness`**, so it defaulted into `/health/readiness` (Micronaut's own default for an unqualified indicator) — the opposite of its own javadoc's stated intent. Worse: it called raw `dataSource.getConnection()` + try-with-resources close, which throws `NoConnectionException` on `close()` under `micronaut-data-jdbc` outside a managed connection scope — **every check leaked a physical connection**, permanently checked-out, never returned to the pool. Fixed with `@Liveness` + `ConnectionOperations<Connection>#executeRead` (`9168b37`). Found by chasing `SbusSecurityIT.exposesReadinessAnonymously`/`DependencyReadinessIT`'s 3 outage-baseline assertions failing 100% reproducibly (not a flake — confirmed via isolated re-runs with debug instrumentation reading the actual `/health/readiness` JSON body, then reverted before committing).
  4. **Two stale test assertions, both root-caused and fixed (`4f6040f`)**: `RecoverableDeadLetterIT.crashAfterBrokerAckReclaimsAndRepublishesSameIdentity` still expected a literal fake `traceparent` to survive an outbox republish, but commit `200dab4` (same-session-prior work, before this continuation) intentionally made every outbox publish stamp a *fresh* W3C traceparent (OBS-02, independently confirmed correct by `OutboxTraceContextIT`, which already tests exactly that) — the commit updated production code but missed this one assertion; fixed to check format instead of the stale literal. `OutboxBatchResilienceIT.aRowReclaimedWhileItsSendIsInFlightNeverFalselyCountsAsPublished` used a class-wide `sbus.outbox.lease: 1ms`; since every reclaim in that class is actually driven by an explicit `ageClaim()` (`now()-5s`), the 1ms lease gave `renewClaim`'s fresh timestamp no real scheduling margin, so under real JVM/DB jitter the row that was *never* meant to be reclaimed (freshly renewed, not aged) could also cross the 1ms staleness threshold before the reaper ran, wrongly reclaiming it too and zeroing the expected published-count. Bumped to `1s` — safe for both reclaim tests in the class, neither of which relies on the bare lease value.
  5. **`SbusMetrics.init()` (`@PostConstruct`) let a Postgres outage abort the whole application boot (`c0e54cf`)**: it ran `refreshCachedCounts()` (3 real outbox queries) synchronously and uncaught — a `@PostConstruct` failure aborts the entire Micronaut context, so this crash-looped the whole SBUS instance on a DB outage at boot instead of starting degraded and self-healing on the next 15s scheduled refresh (the resilience posture the rest of the outbox/reaper code deliberately implements — RES-05/06). Root-caused via `PostgresOutageRetryBudgetIT`'s full stack trace (deliberately designed to boot despite an unreachable Postgres — `initialization-fail-timeout: -1`, Flyway disabled — yet failing at eager bean construction): `PaymentRequestedConsumer`'s DI chain constructs `SbusMetrics`, whose `init()` was the actual thing timing out against the pool. Fixed by catching+logging in `init()` only (`refreshCachedCounts()` itself still throws normally for its own callers — the `@Scheduled` tick and the existing cache-behavior unit tests). New regression test proves both the degraded boot and the subsequent recovery. Verified this fix is unrelated to (does not affect) the `HikariPoolHealthIndicatorIT` gap below — reran it in isolation after this fix, byte-identical failure.
- **Known open gaps — do not re-chase without a new angle, do not block T49/T50/Verifier**:
  1. `HikariPoolHealthIndicatorIT` (the indicator's own dedicated test, `maximum-pool-size=1`) still fails 100% reproducibly, isolated: Flyway's own connection request at boot times out (`HikariPool-N - Connection is not available, request timed out after 30000ms (total=1, active=1, idle=0, waiting=0)`, `total=1, active=1` — something already holds the pool's one connection before Flyway's own first attempt). Confirmed identical in the untouched baseline (predates this session entirely) and confirmed unaffected by the `SbusMetrics` fix above (same byte-identical error after that fix). Ruled out: HikariCP's eager `minimum-idle` warm-up racing Flyway (`minimum-idle: 0` made zero difference). Root cause not found — needs either a real debugger attached or testing on a non-sandboxed host.
  2. `PaymentRequestedConsumerConcurrencyIT.withThreeThreadsThreeDistinctKeyedMessagesOnThreePartitionsProcessConcurrently` is an intermittent timing flake (self-calibrated: batch of 3 concurrent messages must take <2.5x a single-message baseline) — passed in one full-suite run this session, failed (2.8x) in another and again in isolation. Consistent with this sandbox's independently-documented general slowness/contention (see environment note below) plus Awaitility's default 100ms poll granularity being non-trivial relative to the sub-200ms baseline being measured; deliberately NOT loosened, since doing so without a slower/more representative environment to validate against risks masking a real future regression instead of just this sandbox's noise.
- **Environment note**: this sandbox's container restarted unusually often this session (5+ times in under an hour, previously only "twice in a whole session" per the lesson below) — confirmed via `dmesg`/`uptime` to be a full VM/container restart (not OOM: 15GB free, no cgroup limit; not caused by test load). Cause is external to this container, not diagnosable or fixable from inside it. Recovery each time: `docker info` to check, clear stale `containerd.pid`/sockets under `/run/docker/containerd/` if `dockerd` reports `"containerd is still running"` with a dead PID, restart `dockerd`, `docker container prune -f` (leftover ephemeral Testcontainers accumulate fast), re-check `docker ps` for which named containers survived (`restart: unless-stopped` brings most back automatically; anything you'd manually `docker stop`ped stays stopped), re-arm the watchdog Monitor.
- **Also confirmed this session**: running the live docker-compose verification stack (sandbox + gateway + all 4 apps, ~11 containers) *concurrently* with a boundary's own `./gradlew test -PwithIT` (which spins up its own independent Postgres/Kafka/Registry/Redis Testcontainers per test class) causes real resource contention and container-startup timeouts (saw an Apicurio Testcontainers timeout that did not reproduce once the compose stack was brought down first). **Bring the compose stack down before running any boundary's `-PwithIT` suite.**
- **Next step**: finish T49 (remaining boundaries' `-PwithIT`, triage any further real-vs-flake failures the same way, update `docs/architecture-review-2026-08.md` marking every finding resolved with a commit pointer, then the CI-policy/governance/sandbox-config stages — already green, just need re-confirming after any fixes), then T50 (bring the compose stack back up, re-run the AD-007 capacity gate, commit the dated report), then the mandatory Verifier sub-agent (never before T50, never skipped).
- **Blockers**: none that block progress — the `HikariPoolHealthIndicatorIT` gap above is tracked, not blocking.
- **Uncommitted files**: none — `git status --porcelain` clean after each commit above.
- **Branch**: `claude/tlc-spec-driven-continuation-f7t6hp`, pushed, tracking `origin`. No PR opened yet this session (not asked for).

### Operational lessons from this session (read before resuming or dispatching more batches)

1. **This sandbox's whole container restarts unpredictably** (independent of Docker's own instability, seen previously) — it happened twice in one session, each time silently killing every background Bash/Monitor/Agent task with no warning, though on-disk file edits and git state survive intact. On resume: `git status`/`git log` first to see exactly what survived, `docker info` to check the daemon, and re-arm the watchdog Monitor (below) before doing anything else — never assume a background task that stopped notifying is still running.
2. **The Docker daemon dies under sustained IT load AND under container restarts**, and after a restart the stale `/var/run/docker.pid` (pointing at a PID that no longer exists) makes a plain `dockerd` restart fail with `"process with PID ... is still running"` — `rm -f /var/run/docker.pid` before restarting fixes it. Manual restart: `rm -f /var/run/docker.pid; nohup dockerd > /tmp/dockerd.log 2>&1 & disown; sleep 8`. Watchdog Monitor (handles both the stale-pid case and ordinary death): `while true; do docker info >/dev/null 2>&1 || { rm -f /var/run/docker.pid; nohup dockerd >/tmp/dockerd.log 2>&1 & disown; sleep 8; }; sleep 15; done` — does not survive a container restart, re-arm every time.
3. **User direction this session (2026-08-17): defer ALL Docker-dependent verification (`-PwithIT` Testcontainers gates, `make config`'s live-Docker smoke checks, etc.) to ONE consolidated pass at the very end of the feature**, given how often Docker/the container were dying mid-gate and wasting the work. Every task from T21 onward was implemented with full test coverage (including `*IT.java` files) but gated only on `./gradlew test --no-daemon` (compile + unit, no Docker) — each such task's `tasks.md` checkbox carries a parenthetical noting the deferral. **T49/T50 (or a dedicated pre-Verifier pass) must run the full `-PwithIT` suite on every boundary plus any live Docker smoke checks before the feature can honestly be called done** — this is not yet done and is the single biggest remaining risk: a real regression could be hiding in any of the ~25 tasks implemented this way.
4. **`payment-api` gradle runs need `PAYMENT_API_KEY` and `JWT_SIGNATURE_SECRET` exported** (`application.yml` has no default for either). `export PAYMENT_API_KEY=ci-only-required-value JWT_SIGNATURE_SECRET=ci-only-signing-value-with-at-least-32-bytes` before running `./gradlew test` in a *fresh* shell. `payment-sbus` needs nothing extra.
5. **Recovery playbook for a worker's uncommitted/stashed WIP** (used live for T12, T16, and T21-T23 this session): (a) read the diff, judge if implementation+test look finished; (b) if a test fails or is missing, diagnose/write before touching anything else — check for signature/contract breaks in *other*, seemingly-unrelated test files too (T23's WIP silently broke 2 other test files' compilation by changing a shared record's arity — `git status` alone didn't reveal this, only actually compiling did); (c) re-run the *whole module's* gate; (d) do the Test Adequacy Review inline; (e) mark `tasks.md` + `spec.md`, one atomic commit per task, push. When a worker's WIP spans multiple tasks' files interleaved in the same uncommitted tree, split into per-task commits by `git restore`-ing shared files (`tasks.md`/`spec.md`) to HEAD and re-applying just that task's edit before each commit — never `git add` a multi-task diff in one shot.
6. **Batch workers are explicitly authorized to `git commit` AND `git push` (never force)** directly to this branch after each task's gate passes — restate this in every batch-dispatch prompt.
7. **`gradlew ... | tail -N` reports `tail`'s exit code, not gradle's** — use `${PIPESTATUS[0]}`/`set -o pipefail` and independently check `build/test-results/test/*.xml` counts.
8. Remaining work: **T46** (WIP stashed, pick up first) → **T47** (docs) → **T48-T50** (Phase 9: ADRs/versioning, workspace gates, AD-007 capacity re-run) → **the consolidated Docker/`-PwithIT` verification pass deferred per lesson 3** → the mandatory feature-level Verifier sub-agent (after T50, never before).

### Prior Handoff (superseded 2026-08-16, kept for adversarial-audit-fixes detail)

- **Feature**: `adversarial-audit-fixes` — **closed**. All 24 tasks done and committed (`5f1d23f`..T24's commit below), all 27 AUD-xx requirements Verified with commit pointers in the traceability table, and `scripts/verify-workspace.sh` (all 8 stages) passed live on the rebuilt images.
- **Phase / Task**: Everything closed. No open feature, no open follow-up task.
- **What Phase 5 did (T21-T23, commits `28ce946`, `d00da78`, `5b5d6fe`)**: registered AD-007 superseding AD-006 (target 10,000→1,000 req/min sustained, spike 20,000→2,000 req/min, new latency targets avg≤300ms/p99≤10s, admission 200/50→20/10 req/s route/tenant); recalibrated `payment-api/src/main/resources/application.yml` and docs to match. Fixed the capacity gate itself (AUD-30): `load/k6/capacity.js` now distributes load across 2 tenant API keys instead of one, and `load/capacity/generate_report.py`'s verdict now fails when `429`s exceed 1% of the steady scenario (proven with a synthetic bad-mix fixture — the old logic would have passed the exact shape of the revoked 2026-08-12 report: 0 technical errors, 70% `429`). **Live run caught a real bug the DRYRUN missed**: the first full 15-minute run showed 16.53% `429` in steady even though the offered rate was under both budgets — root cause was the tenant-key picker (`(VU+ITER) % 2`) correlating with k6's `constant-arrival-rate` scheduling, clustering whole 1-second windows onto one tenant despite an even run-wide split; fixed by picking the tenant from `exec.scenario.iterationInInstance` (true chronological global sequence). Re-ran live: steady (17.00 req/s × 15min, 15,301 requests) — `429=0`, technical errors 0, reconciliation 153/153 terminal, avg latency 293.69ms, p99 421.65ms, tenant split 7651/7650. Spike (33 req/s × 60s) — `429=769/1981` (38.8%, expected/not gated), 0 technical errors. Report: `load/reports/20260814-123447-capacity-report.md`. CAP-02 in `repository-segregation-production-hardening/spec.md` marked REVOGADO with the reason (measured a single tenant's rate-limit bucket, not system capacity) and now points to this new evidence.
- **T24 closure**: hit a genuine, host-level Docker Desktop outage mid-task (`/System/Volumes/Data` at 100% capacity, 2.2GiB free — every `docker` CLI call, including read-only ones, hung for 10+ minutes) while bringing the compose stack back up after the capacity gate; resolved externally (39GB of an unrelated `~/Library/Application Support/PatchKit/` cache freed with the user's approval, Docker Desktop restarted, containers manually recreated, ~50 leftover ephemeral Testcontainers pruned). Once Docker recovered, the live `scripts/verify-workspace.sh` run **caught a second real bug**, unrelated to the outage: the RED-01 `status-lifecycle-all-states` scenario's short-TTL scratch container (`scripts/e2e/async-redis-failures/scenarios/status_lifecycle.sh`) set `ASYNC_REDIS_RESULT_TTL=3s`/`ASYNC_REDIS_STATUS_TTL=8s` but never lowered `ASYNC_REDIS_IDEMPOTENCY_TTL` from its 24h default — so T20's own AUD-20 startup guard (`status-ttl >= idempotency-ttl`) correctly refused to boot it, and the container never became healthy (reproduced twice, confirmed non-transient by manually running the exact `docker run` and reading its crash log). Fixed by adding `-e ASYNC_REDIS_IDEMPOTENCY_TTL=8s` to the scenario's container — a test-fixture gap this feature's own T20 fix exposed, not a fix to T20 itself. Confirmed clean: `async-redis-failures: 34/34 assertions passed`. Full re-run of all 8 stages afterward, clean: `equivalence: PASS (436 entries)`, `no-composite-build: PASS (4 boundaries)`, `artifact-only-consumer: PASS`, e2e-payment `SMOKE OK (COMPLETED)`, e2e-async-redis `SMOKE OK (COMPLETED)`, `payment-failures: PASS (11/11 scenarios)`, `async-redis-failures: PASS (34/34 assertions)`, `hygiene: git diff --check` clean. `verify-workspace: PASS (stage=all)`.
- **In-progress**: none — working tree is clean after T24's commit.
- **Next step**: none required. Docker state: `/sandbox` core + observability infra up and healthy; all 4 app containers (`payment-api`, `payment-sbus`, `payment-core-mock`, `async-redis-service`) up and healthy on the `:local` images rebuilt this session (every fix from T1-T20 baked in). Stop with `docker compose --env-file .env down` per boundary if not needed for further work.
- **Blockers:** none currently. Worth a watch item, not a task: the host disk that wedged Docker mid-session was down to 2.2GiB free before the user's cleanup: freed to ~42GB via one 39GB unrelated cache deletion, but this project's own docker context alone still carries ~40GB of reclaimable images/build cache (`docker system df`, checked pre-outage) that will recur as churn continues — a periodic `docker builder prune`/`docker image prune` is worth doing proactively rather than waiting for the disk to hit the wall again.
- **Uncommitted files**: none — working tree is clean (`git status --porcelain` empty) after T24's commit.
- **Branch**: `feature/plan-continue`. Not pushed or merged — that decision is the user's, not made here.

### Prior Handoff (superseded 2026-08-14, kept for repository-segregation-production-hardening/residual-resilience-findings detail)

- **Feature**: none in progress. `residual-resilience-findings` closed (Verifier PASS). `task_40100c4c` (dependency CVE bump) closed same-session, direct execution (no formal spec.md/tasks.md — a mechanical per-boundary bump-and-verify task, tracked via TaskCreate/TaskUpdate instead). Base feature `repository-segregation-production-hardening` was already closed prior — see archived detail below.
- **Phase / Task**: **Everything closed.** No open feature, no open follow-up task.
- **`task_40100c4c` closure (2026-08-13, commits `25f1d9b..4cf683f`)**: coordinated dependency bump across `payment-contracts` + the 4 scanned app boundaries (`payment-api`, `payment-sbus`, `payment-core-mock`, `async-redis-service`) to close the 25-31 HIGH/CRITICAL Trivy findings per image T60 had recorded as an honest FAIL. `avroVersion` 1.11.3→1.11.4 (`CVE-2024-47561`); each app boundary's `micronautVersion` 4.7.6→4.10.9 plus forced `netty-bom` 4.2.17.Final, `kafka-clients` 3.9.2, `io.micronaut:*` 4.10.26, Jackson 2.21.5, Micrometer 1.16.6 (payment-sbus also `postgresql` 42.7.8→42.7.13) — the platform BOM's own aggregate release lags individual modules' patch cadence, so exact fixed versions were confirmed resolvable via live Gradle dependency resolution against Maven Central (the fetch tool was 403'd against `repo.maven.apache.org`/`mvnrepository.com` directly — Gradle resolution against the real repo was used as ground truth instead). Full regression green on every boundary (134/86/27/101 tests). The `async-redis-service` bump surfaced a real, confirmed regression (intermittent `ServerStartupException` on the fixed test port 8084 — a known Micronaut `@MicronautTest` issue class, confirmed via a reverted-bump baseline re-run: 101/101 clean without the bump, 5-9 flaky failures with it) — fixed with `src/test/resources/application-test.yml` (`micronaut.server.port: -1`, test-only, standard Micronaut fix). Every image rebuilt and re-scanned: **0 Java/JVM findings remain on any image** (was 25-31 each); 3 residual findings per image are Alpine OS-package CVEs (`libexpat`, `p11-kit`) with no newer `eclipse-temurin:21-jre-alpine` digest published upstream yet (confirmed via fresh pull, same digest already pinned) — a watch item, not an open task. `feature-control` deliberately left untouched: it builds no Docker image, was never in Trivy's scanned surface, and its consumers already force safe versions on their own resolved classpath regardless of what it requests internally. Full `scripts/verify-workspace.sh` (all 8 stages) re-run clean afterward on the rebuilt images. Evidence: `.specs/features/repository-segregation-production-hardening/validation-evidence/2026-08-13-cve-remediation.md`.
- **In-progress**: none — working tree is clean, no task is mid-flight.
- **Next step**: none required. Docker state: `/sandbox` core + observability infra is up; all 4 app containers (`payment-api`, `payment-sbus`, `payment-core-mock`, `async-redis-service`) are up and healthy, rebuilt with the CVE-remediated images from this session; stop with `docker compose --env-file .env down` per boundary if not needed for further work.
- **Blockers:** none. Every follow-up task from the post-feature review and the subsequent live-verification pass is now resolved: `task_fc6d987b`/`task_02ac42a5` (payment-sbus, `44c71de`), `task_ba7dd0af` (async-redis, `e237f28`), `task_9b6bb521` (payment-api `/v0`, `9ebf152`), `task_bca58451` (capacity gate, `b95deae`), `task_5a0df80c` (payment-api Redis fail-closed, `residual-resilience-findings` feature), `task_40100c4c` (dependency CVEs, this entry). Nothing standing.
- **Uncommitted files**: none — working tree is clean (`git status --porcelain` empty).
- **Branch**: `feature/plan-continue`. Not pushed or merged — that decision is the user's, not made here.

### Prior Handoff (superseded 2026-08-13, kept for residual-resilience-findings detail)

- **Feature**: `residual-resilience-findings` (closed). Base feature `repository-segregation-production-hardening` was already closed prior — see archived detail further below.
- **Phase / Task**: **Feature complete.** All 8 tasks (T1–T8) done, and the feature-level Verifier (fresh sub-agent, author ≠ verifier) returned **PASS**. `python3 <skill-dir>/scripts/validate_state.py residual-resilience-findings` → `0 error(s)`. Nothing further required.
- **What it fixed**: a live run of `scripts/verify-workspace.sh payment-failures` surfaced 2 of the 11 failure-matrix scenarios red. (1) `payment-api`'s `RedisStatusStore` leaked raw Lettuce connection errors (host/port in the HTTP body) as a `500` instead of failing closed — fixed with a new `StoreUnavailableException`/`StoreUnavailableExceptionHandler` (503, no infra detail), `RedisStatusStore`'s 5 public methods now translate `RedisException` instead of letting it escape, and `ApiPaymentService.getStatus` keeps consulting the SBUS durable fallback during a Redis outage rather than failing immediately — only 503s when *neither* store can answer, never a false `404`. `PaymentResponseConsumer` was independently re-verified as already fail-closed-correct (no fix needed there, only added the missing apply-stage test coverage — see `spec.md` Assumptions for the corrected finding). (2) `scripts/e2e/payment-failures/scenarios/crash_recovery.sh`'s `outbox-crash-window-reclaim` setup only looked for a `payment.simulation.completed` outbox row, so it failed at random whenever the mock Core declined (terminal topic `.failed` instead) — fixed to accept both. Live re-run after the fix: **`payment-failures: 11/11`** (was 9/11), evidence in `spec.md`'s "Evidência da execução ao vivo" section.
- **This closed `task_5a0df80c`** (`RedisStatusStore` not failing closed on Redis outage — a standing follow-up first flagged by T59/T60/CAP-02 re-verification in the base feature).

### Prior Handoff (superseded 2026-08-13, kept for repository-segregation-production-hardening detail)

- **Feature**: `repository-segregation-production-hardening`
- **Phase / Task**: **Feature complete.** Phases 1–8 (T1–T53) and Phase 9 (T54–T60, workspace closeout) are all done, and the feature-level Verifier (author ≠ verifier, the skill's mandatory closing gate) has returned **PASS**. `python3 <skill-dir>/scripts/validate_state.py repository-segregation-production-hardening` → `0 error(s)`. Nothing further is required to consider this feature done.
- **Completed**: T1–T60, plus the mandatory Verifier cycle. T54–T59 detail preserved unchanged below. T60 (`e7a6036`): ran the 6 Java boundaries' Quick+Full gates live, all green (`payment-contracts` 28/28, `payment-api` 68/119, `payment-sbus` 44/76, `payment-core-mock` 21/27, `feature-control` 113/141, `async-redis-service` 39/96); reproduced CI's SBOM+Trivy tooling live against all 4 app images and recorded a real, honest FAIL (25-31 HIGH/CRITICAL CVEs per image, fixes available upstream, tracked as `task_40100c4c`) rather than promoting it to PASS. **Verifier iteration 1** (fresh sub-agent, no authoring context) then ran the mandatory post-completion gate and returned **FAIL** with 7 real, grounded findings: 2 blockers (CAP-02 had no committed post-fix evidence artifact; T59 had orphaned `validate_docs.py`'s dashboard-metric scanner at the deleted legacy path, making EDG-04 silently vacuous — scanning 0 of 10 real dashboards), 2 majors (stale `scripts/observability/verify-evidence.txt`; a surviving mutant — `SimulationMessageHandler`'s MDC wiring had no call-site test, only the utility's own unit test), 3 minors (stale ORG-08 entry count, dead `LEGACY_TRANSITIONAL_ROOTS` code, `feature-control`'s CI matrix excluding the library's own 20 ITs). Full report: `validation.md` history via `git log -p`, current version reflects iteration 2. **Fixed all 7** (`c8fff22`): re-ran the full `certified-target/steady` capacity scenario live (167 req/s×15min, 150,301 requests) — technical error rate now **0.0000%** (was 4.1-44.4%), proving `task_3801253b`'s fix under real sustained load with a committed dated artifact (`load/reports/20260812-095631-capacity-report.md`); the automated report's own verdict is still FAIL on a separate "zero silent loss" sub-criterion (2/185 samples) — investigated via direct Postgres query and found to be a false negative in the gate's own reconciliation methodology (Redis `status-ttl: 15m` exactly equals the scenario's own 15m duration, so early samples' status entries expire before reconciliation checks them at the end; both samples actually completed in ~1s) — documented honestly in `load/reports/20260812-095631-cap02-addendum.md`, methodology fix tracked as `task_bca58451` rather than patched inline; fixed `validate_docs.py#dashboard_metric_errors`'s glob to the 3 real dashboard locations (mirroring `equivalence.py`'s already-fixed one) and re-pointed `dashboard_validator.sh`'s fixture path; added `SimulationMessageHandlerUnitTest.handlingARequestedEventPopulatesMdcWithTheEnvelopeCorrelationIdsWhileProcessing` (captures MDC via `doAnswer` on the mocked service during the call, empirically verified to kill the mutation in an isolated `git worktree`); regenerated `verify-evidence.txt` (12/12 live); fixed ORG-08 (389→409); removed the dead `LEGACY_TRANSITIONAL_ROOTS` constant + its now-vacuous test; fixed `ci.yml`'s feature-control integration command and `check_ci_policy.py`'s matching policy string. **Verifier iteration 2** (fresh sub-agent again) independently re-ran every one of the 7 fixes (not just read the claims — re-ran gates, re-derived the discrimination sensor itself in its own scratch worktree, re-queried Postgres and got byte-identical rows) and returned **PASS**, with one new Minor finding (the `CORE_RESPONSE` branch of the same MDC wiring also lacked a call-site test — a narrower instance of the same gap). Fixed immediately (`215217d`): added `handlingACoreResponseEventPopulatesMdcWithTheEnvelopeCorrelationIdsWhileProcessing` and widened the REQUESTED-branch test to also assert `correlationId`/`causationId`/`traceId` (previously only checked `requestId`). Did not re-dispatch a 3rd Verifier for this — iteration 2's own verdict was already PASS despite noting this Minor item, and the fix is a direct, verified extension of the exact pattern iteration 2 just validated. Distilled 1 lesson (`L-001`, surviving-mutant class: a utility's own unit test doesn't prove its call sites are wired) via `lessons.py` — `.specs/LESSONS.md`/`lessons.json` now exist for this project for the first time.
- **Post-feature review (2026-08-12, 4 commits `176a3e5..bfd37cf`)**: after the feature closed, a five-dimension independent review (legacy leftovers, gate vacuity, application code, doc accuracy, structure/governance) found and fixed defects the feature's own gates had missed. The two most serious were invisible precisely because a gate was reading stale state: (1) `feature-control`'s publication wrote to `library/build/repo` while every consumer defaulted to `feature-control/build/repo` — a directory only still populated by a leftover artifact from 2026-08-09, so `payment-api` had been compiling against a **pre-FTR-02/03/04 jar** for days and the artifact-only gate was passing on that same stale copy; fixed by anchoring the publication on the boundary root (matching payment-contracts' convention), which immediately surfaced that `FeatureAdminController` called the removed 1-arg `put`/`delete` and had to be moved to the CAS API. (2) `.github/workflows/publish.yml` still invoked the deleted root `gradlew`, so every tag release would have failed — never caught because `check_ci_policy.py` only ever opened `ci.yml`. Also fixed: the root CI could not pass on a clean checkout (no step published the cross-boundary artifacts the consumer roots resolve by GAV); `validate_docs.py` scanned only `docs/` so its port/variable/metric/claim sub-checks examined **zero items** while printing PASS (now scans the whole workspace, boundary-aware, with a floor that fails when a sub-check examines nothing); `check_repository_secrets.py` was **red** on committed dev secrets and blind to every `compose.yml`/`compose.yaml` (fixed both, plus the same dev-secret-as-default pattern in payment-sbus and async-redis-service, which had escaped only by variable naming); `async-redis-service` exposed `POST /stop`, `/env`, `/beans`, `/threaddump` anonymously via `endpoints.all.enabled: true`; `payment-api`'s `rate-limit` block sat under the wrong property prefix, making `PAYMENT_API_INSTANCES` inert; `AsyncRateLimiter`'s degraded path counted the fleet-wide budget locally (N× the approved burst, the exact failure its own javadoc promised to prevent). Plus: `deploy/` moved under `sandbox/` (AD-003), duplicate/orphan k6 scripts relocated to their owners, 7 orphaned `TODO verify` markers resolved, 4 Javadocs and several doc claims corrected against the real code, `run-with-composite.sh` retired (composite substitution is now structurally impossible), dead code removed (`markCompleted`, `SbusMessageStatus.RECEIVED`). Full workspace gate re-run live afterwards: 8/8 stages PASS.

**`task_89c681c8` fix (outside the T1–T60 sequence, a follow-up to T58's finding, same session):** `SimulationMessageHandler` (`payment-sbus/src/main/java/com/example/payments/sbus/kafka/SimulationMessageHandler.java`) now calls `Mdc.fromConsumer(record, env)` before business processing and `Mdc.clear()` in a `finally` — `handle()`'s signature changed to take the whole `ConsumerRecord` instead of separately-extracted `value`+`headers`, which also let `PaymentRequestedConsumer`/`CoreResponseConsumer`/`RetryConsumer` drop their now-redundant header extraction before the call. `ApiPaymentService.publishAndAwait` and `PaymentResponseConsumer.apply` (payment-api) now put `causationId` into MDC. New tests: `MdcUnitTest` (payment-sbus), `ApiPaymentServiceUnitTest.populatesCausationIdInMdcWhilePublishing`. `./gradlew test -PwithIT` green on both boundaries. Re-ran `scripts/observability/verify.sh` live after rebuilding both images: **12/12 checks pass** (previously 10/12).

**`task_3801253b` fix (outside the T1–T60 sequence, a follow-up to T57's finding, same session):** `ResponseCoordinator.onMessage()` (`payment-api/src/main/java/com/example/payments/api/coordination/ResponseCoordinator.java`) now dispatches to a virtual thread instead of running `complete()` inline on Lettuce's PubSub event-loop thread — a slow Redis command there used to block delivery of every other instance's completion notifications, not just the current one. New `RedisClientTuning` (`payment-api/src/main/java/com/example/payments/api/redis/RedisClientTuning.java`, `@Context` bean) bounds every Lettuce command to 2s instead of the client's 60s default. Two new regression tests in `ResponseCoordinatorUnitTest` prove a stuck notification for one requestId never delays another's; `RedisClientTuningUnitTest` proves the bounded timeout. `./gradlew test -PwithIT` on payment-api: 118/118 green. Re-ran the exact live reproduction from T57 (167 req/s×90s against `certified-target`) after rebuilding the image: 0 `RedisCommandTimeoutException` in the container logs (previously recurring every ~60s), 0 technical errors (previously 40-44%).

### Prior Handoff (superseded 2026-08-10, kept for T1–T40 detail)
